node_modules/
dist/
site/dist/
.DS_Store
*.local
.env
.env.*
!.env.example

# SQLite databases created by `bun run db:migrate` and by tests
*.db
*.db-wal
*.db-shm

# WXT build output and generated types
extension/.output
extension/.wxt

# Design preview output, regenerated on demand.
design-preview.png

# Account-specific operations log. Kept locally; see deploy/README.md.
gcp.md

# Chrome for Testing, installed by @puppeteer/browsers for browser-e2e.
chrome/

# Design source bundles and raw media deliverables. Large, and build inputs rather
# than sources: the optimised assets that ship live under site/.
docs/design/
